package semantic.utils.enums;

public enum Sentence {
    CONST_DEF,
    VAR_DEF,
    VARDEF_ASSIG,
    ASSIG,
    IF,
    IF_ELSE,
    ELSE,
    FOR,
    WHILE,
    DO_WHILE,
    INNER_FUNCT_CALL,
    OUTER_FUNCT_CALL,
    RETURN
}
